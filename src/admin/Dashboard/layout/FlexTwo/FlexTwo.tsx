import './FlexTwo.scss';

const FlexTwo = (props: {
    children: React.ReactNode
}) => {
    return (
        <div className="stachesepl-flex-two">{props.children}</div>
    )
}

export default FlexTwo;